<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Finance Automation Specialist. Transforming manual finance processes into high-speed automated pipelines using Python and AI.">
    <title>KK Tan | AI & Financial Automation</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&family=JetBrains+Mono:wght@400;700&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
    
    <style>
        /* === 保留了你所有的原始 CSS 样式，未做任何删减 === */
        :root {
            --bg-base: #050B14;
            --bg-surface: rgba(15, 23, 42, 0.6);
            --bg-surface-hover: rgba(30, 41, 59, 0.8);
            --accent-gold: #FFB84D;
            --accent-gold-glow: rgba(255, 184, 77, 0.4);
            --accent-cyan: #00E5FF;
            --accent-cyan-glow: rgba(0, 229, 255, 0.3);
            --text-main: #F8FAFC;
            --text-muted: #94A3B8;
            --font-sans: 'Inter', sans-serif;
            --font-display: 'Space Grotesk', sans-serif;
            --font-mono: 'JetBrains Mono', monospace;
            --max-width: 1200px;
            --glass-border: 1px solid rgba(255, 255, 255, 0.08);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }
        body {
            font-family: var(--font-sans); color: var(--text-main);
            background-color: var(--bg-base); line-height: 1.6; overflow-x: hidden;
            background-image: 
                linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
            background-size: 30px 30px; background-position: center center;
        }

        .ambient-glow { position: fixed; border-radius: 50%; filter: blur(100px); z-index: -1; pointer-events: none; }
        .glow-1 { top: -10%; left: -10%; width: 50vw; height: 50vw; background: radial-gradient(circle, var(--accent-cyan-glow) 0%, transparent 70%); }
        .glow-2 { bottom: -10%; right: -10%; width: 60vw; height: 60vw; background: radial-gradient(circle, var(--accent-gold-glow) 0%, transparent 70%); }
        a { text-decoration: none; color: inherit; }
        ul { list-style: none; }
        .container { max-width: var(--max-width); margin: 0 auto; padding: 0 24px; }

        h1, h2, h3 { font-family: var(--font-display); line-height: 1.2; letter-spacing: -0.02em; }
        .section-title {
            font-size: clamp(1.8rem, 6vw, 2.5rem); word-break: break-word; margin-bottom: 3rem; text-align: center;
            background: linear-gradient(to right, var(--text-main), var(--text-muted));
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            position: relative; display: inline-block; left: 50%; transform: translateX(-50%);
        }
        .section-title::after {
            content: ''; position: absolute; bottom: -10px; left: 50%; transform: translateX(-50%);
            width: 40px; height: 2px; background: var(--accent-gold); box-shadow: 0 0 10px var(--accent-gold);
        }

        .glass-panel {
            background: var(--bg-surface); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
            border: var(--glass-border); border-radius: 16px;
        }

        .btn {
            display: inline-flex; align-items: center; justify-content: center; padding: 12px 28px;
            border-radius: 8px; font-family: var(--font-mono); font-size: 0.9rem; font-weight: 700;
            text-transform: uppercase; letter-spacing: 0.05em; cursor: pointer; transition: all 0.3s ease;
            position: relative; overflow: hidden; z-index: 1;
        }
        .btn-gold { background: rgba(255, 184, 77, 0.1); color: var(--accent-gold); border: 1px solid var(--accent-gold); box-shadow: 0 0 15px rgba(255, 184, 77, 0.1); }
        .btn-gold:hover { background: var(--accent-gold); color: var(--bg-base); box-shadow: 0 0 25px rgba(255, 184, 77, 0.4); transform: translateY(-2px); }
        .btn-cyan { background: rgba(0, 229, 255, 0.1); color: var(--accent-cyan); border: 1px solid var(--accent-cyan); box-shadow: 0 0 15px rgba(0, 229, 255, 0.1); }
        .btn-cyan:hover { background: var(--accent-cyan); color: var(--bg-base); box-shadow: 0 0 25px rgba(0, 229, 255, 0.4); transform: translateY(-2px); }

        nav { position: fixed; top: 0; left: 0; width: 100%; z-index: 1000; background: rgba(5, 11, 20, 0.8); backdrop-filter: blur(16px); border-bottom: 1px solid rgba(255,255,255,0.05); }
        .nav-container { display: flex; justify-content: space-between; align-items: center; height: 80px; }
        .logo { font-family: var(--font-display); font-size: 1.5rem; font-weight: 700; display: flex; align-items: center; gap: 10px; }
        .logo-icon { color: var(--accent-cyan); font-size: 1.2rem; animation: pulse 2s infinite; }
        .nav-links { display: flex; gap: 2.5rem; }
        .nav-links a { font-family: var(--font-mono); font-size: 0.9rem; color: var(--text-muted); transition: color 0.3s, text-shadow 0.3s; }
        .nav-links a:hover { color: var(--accent-gold); text-shadow: 0 0 8px var(--accent-gold-glow); }
        .hamburger { display: none; background: none; border: none; color: var(--text-main); font-size: 1.8rem; cursor: pointer; }

        #home { min-height: 100vh; display: flex; align-items: center; padding-top: 80px; position: relative; }
        .hero-content { max-width: 800px; position: relative; z-index: 10; }
        .status-badge { display: inline-flex; align-items: center; gap: 8px; padding: 6px 12px; background: rgba(0, 229, 255, 0.1); border: 1px solid rgba(0, 229, 255, 0.2); border-radius: 20px; font-family: var(--font-mono); font-size: 0.8rem; color: var(--accent-cyan); margin-bottom: 2rem; }
        .status-dot { width: 8px; height: 8px; background: var(--accent-cyan); border-radius: 50%; box-shadow: 0 0 8px var(--accent-cyan); animation: blink 1.5s infinite; }
        .hero-title { font-size: 4.5rem; margin-bottom: 1rem; background: linear-gradient(135deg, #fff 0%, #94A3B8 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .hero-subtitle { font-family: var(--font-mono); font-size: 1.5rem; color: var(--accent-gold); margin-bottom: 1.5rem; min-height: 2.4rem; }
        .cursor { display: inline-block; width: 10px; height: 1.2em; background-color: var(--accent-gold); vertical-align: middle; animation: blink 1s step-end infinite; }
        .hero-desc { font-size: 1.1rem; color: var(--text-muted); margin-bottom: 3rem; max-width: 600px; }
        .hero-buttons { display: flex; gap: 1.5rem; }

        #stats { padding: 2rem 0 6rem; position: relative; z-index: 10; }
        .stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; }
        .stat-card { padding: 2rem; text-align: center; transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; position: relative; overflow: hidden; }
        .stat-card::before { content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 2px; background: linear-gradient(90deg, transparent, var(--accent-cyan), transparent); opacity: 0; transition: opacity 0.3s; }
        .stat-card:hover { transform: translateY(-5px); border-color: rgba(0, 229, 255, 0.3); box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5); }
        .stat-card:hover::before { opacity: 1; }
        .stat-number { font-family: var(--font-mono); font-size: 3rem; font-weight: 700; color: var(--text-main); margin-bottom: 0.5rem; text-shadow: 0 0 15px rgba(255,255,255,0.2); }
        .stat-number span { color: var(--accent-gold); }
        .stat-label { font-family: var(--font-mono); font-size: 0.85rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.1em; }

        #about { padding: 6rem 0; }
        .terminal-window { max-width: 900px; margin: 0 auto; background: #0A0F1C; border: 1px solid rgba(0, 229, 255, 0.2); border-radius: 12px; overflow: hidden; box-shadow: 0 20px 50px rgba(0,0,0,0.5); }
        .terminal-header { background: rgba(15, 23, 42, 0.9); padding: 12px 20px; display: flex; align-items: center; border-bottom: 1px solid rgba(255,255,255,0.05); }
        .terminal-dots { display: flex; gap: 8px; }
        .terminal-dots span { width: 12px; height: 12px; border-radius: 50%; }
        .dot-r { background: #FF5F56; } .dot-y { background: #FFBD2E; } .dot-g { background: #27C93F; }
        .terminal-title { margin: 0 auto; font-family: var(--font-mono); font-size: 0.85rem; color: var(--text-muted); }
        .terminal-body { padding: 2.5rem; font-family: var(--font-mono); font-size: 0.95rem; line-height: 1.8; }
        .prompt { color: var(--accent-cyan); margin-right: 10px; }
        .terminal-body p { margin-bottom: 1.5rem; color: #D4D4D8; }

        #projects { padding: 6rem 0; }
        .projects-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; }
        .project-card { display: flex; flex-direction: column; transition: transform 0.3s, border-color 0.3s; }
        .project-card:hover { transform: translateY(-8px); border-color: rgba(255, 184, 77, 0.4); }
        .project-visual { height: 160px; background: linear-gradient(135deg, rgba(15, 23, 42, 0.8) 0%, rgba(5, 11, 20, 0.9) 100%); border-bottom: 1px solid rgba(255,255,255,0.05); display: flex; align-items: center; justify-content: center; font-size: 3.5rem; position: relative; overflow: hidden; }
        .project-visual::after { content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0; background-image: linear-gradient(rgba(0, 229, 255, 0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(0, 229, 255, 0.1) 1px, transparent 1px); background-size: 20px 20px; opacity: 0.3; }
        .project-body { padding: 2rem; flex-grow: 1; display: flex; flex-direction: column; }
        .project-title { font-size: 1.4rem; color: var(--accent-gold); margin-bottom: 1rem; }
        .project-desc { font-size: 0.95rem; color: var(--text-muted); margin-bottom: 1.5rem; flex-grow: 1; }
        .project-tags { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 2rem; }
        .project-tag { font-family: var(--font-mono); font-size: 0.75rem; color: var(--accent-gold); background: rgba(255, 184, 77, 0.05); padding: 4px 10px; border-radius: 4px; border: 1px solid rgba(255, 184, 77, 0.2); }
        .project-links { display: grid; grid-template-columns: 1fr; gap: 12px; }
        .project-links .btn { padding: 10px; font-size: 0.8rem; }

        #contact { padding: 8rem 0; position: relative; }
        .contact-wrapper { max-width: 600px; margin: 0 auto; text-align: center; padding: 4rem 2rem; position: relative; overflow: hidden; }
        .contact-wrapper::before { content: ''; position: absolute; top: 0; left: 50%; transform: translateX(-50%); width: 100px; height: 2px; background: var(--accent-cyan); box-shadow: 0 0 20px var(--accent-cyan); }
        .contact-subtitle { font-family: var(--font-mono); color: var(--text-muted); margin-bottom: 3rem; }
        .contact-buttons { display: flex; flex-direction: column; gap: 1rem; }
        .contact-btn { display: flex; align-items: center; justify-content: center; gap: 12px; width: 100%; padding: 16px; background: rgba(15, 23, 42, 0.6); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 8px; color: var(--text-main); font-family: var(--font-mono); font-size: 1rem; transition: all 0.3s; }
        .contact-btn:hover { background: rgba(255, 184, 77, 0.1); border-color: var(--accent-gold); color: var(--accent-gold); transform: scale(1.02); box-shadow: 0 0 20px rgba(255, 184, 77, 0.15); }

        footer { border-top: 1px solid rgba(255,255,255,0.05); padding: 2rem 0; text-align: center; font-family: var(--font-mono); font-size: 0.85rem; color: var(--text-muted); background: rgba(5, 11, 20, 0.9); }

        @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
        @keyframes pulse { 0%, 100% { opacity: 1; text-shadow: 0 0 5px var(--accent-cyan); } 50% { opacity: 0.5; text-shadow: none; } }

        @media (max-width: 1024px) {
            .stats-grid, .projects-grid { grid-template-columns: repeat(2, 1fr); }
            .hero-title { font-size: 3.5rem; }
        }
        @media (max-width: 768px) {
            .hamburger { display: block; }
            .nav-links { position: absolute; top: 80px; left: 0; width: 100%; background: rgba(5, 11, 20, 0.95); backdrop-filter: blur(16px); flex-direction: column; gap: 0; max-height: 0; overflow: hidden; transition: max-height 0.3s ease-out; border-bottom: 1px solid rgba(255,255,255,0.05); }
            .nav-links.active { max-height: 300px; }
            .nav-links li { text-align: center; border-top: 1px solid rgba(255,255,255,0.05); }
            .nav-links a { display: block; padding: 20px 0; }
            .hero-title { font-size: 2.8rem; }
            .hero-subtitle { font-size: 1.2rem; }
            .hero-buttons { flex-direction: column; }
            .stats-grid, .projects-grid { grid-template-columns: 1fr; }
            .terminal-body { padding: 1.5rem; }
            .contact-wrapper { padding: 2rem 1rem; }
        }
    </style>
</head>
<body>

    <div class="ambient-glow glow-1"></div>
    <div class="ambient-glow glow-2"></div>

    <nav>
        <div class="container nav-container">
            <a href="#home" class="logo"><span class="logo-icon">⚡</span> KK_Tan</a>
            <button class="hamburger" id="hamburger" aria-label="Menu">☰</button>
            <ul class="nav-links" id="nav-links">
                <li><a href="#home">// HOME</a></li>
                <li><a href="#about">// SYSTEM.ABOUT</a></li>
                <li><a href="#projects">// MODULES</a></li>
                <li><a href="#contact">// CONNECT</a></li>
            </ul>
        </div>
    </nav>

    <header id="home">
        <div class="container">
            <div class="hero-content">
                <div class="status-badge"><div class="status-dot"></div>SYSTEM_ONLINE</div>
                <h1 class="hero-title" id="profile-name">Initializing...</h1>
                <h2 class="hero-subtitle">
                    <span id="typewriter-text"><span class="cursor"></span></span>
                </h2>
                <p class="hero-desc" id="profile-desc">Syncing data from core database...</p>
                <div class="hero-buttons">
                    <a href="#projects" class="btn btn-gold">Initialize Modules</a>
                    <a href="#contact" class="btn btn-cyan">Establish Connection</a>
                </div>
            </div>
        </div>
    </header>

    <section id="stats">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-card glass-panel"><div class="stat-number">>10<span>_</span></div><div class="stat-label">Years Exp</div></div>
                <div class="stat-card glass-panel"><div class="stat-number">15<span>+</span></div><div class="stat-label">Projects</div></div>
                <div class="stat-card glass-panel"><div class="stat-number">60<span>%</span></div><div class="stat-label">Efficiency Gain</div></div>
                <div class="stat-card glass-panel"><div class="stat-number">50<span>%</span></div><div class="stat-label">Cycle Time Cut</div></div>
            </div>
        </div>
    </section>

    <section id="about">
        <div class="container">
            <h2 class="section-title">System.About()</h2>
            <div class="terminal-window">
                <div class="terminal-header">
                    <div class="terminal-dots"><span class="dot-r"></span><span class="dot-y"></span><span class="dot-g"></span></div>
                    <div class="terminal-title">kk_tan@automation-engine:~</div>
                </div>
                <div class="terminal-body" id="about-container">
                    <p><span class="prompt">root@kk-tan:~$</span> cat profile.txt</p>
                    <p style="color: var(--accent-gold);">Downloading profile stream...</p>
                </div>
            </div>
        </div>
    </section>

    <section id="projects">
        <div class="container">
            <h2 class="section-title">Deployed_Modules</h2>
            <div class="projects-grid" id="projects-container">
                <div style="grid-column: 1 / -1; text-align: center; padding: 2rem;">
                    <p style="color: var(--accent-cyan); font-family: var(--font-mono);">[ Fetching modules from Google Sheets API... ]</p>
                </div>
            </div>
        </div>
    </section>

    <section id="contact">
        <div class="container">
            <div class="contact-wrapper glass-panel">
                <h2 class="section-title" style="margin-bottom: 1rem;">Initiate_Handshake</h2>
                <p class="contact-subtitle">Awaiting input: Have a finance process you want to automate?</p>
                <div class="contact-buttons">
                    <a href="mailto:kktan@email.com" class="contact-btn"><span class="contact-icon">📧</span> [ EXECUTE: Send_Email ]</a>
                    <a href="https://linkedin.com/in/kktan" target="_blank" class="contact-btn"><span class="contact-icon">🔗</span> [ ACCESS: LinkedIn_Profile ]</a>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <p>System.exit(0); // © 2026 KK Tan. Data driven by Google Sheets.</p>
        </div>
    </footer>

    <script>
        // 【重要】将下面的 URL 替换为你刚才在 GAS 部署生成的 Web App URL
        const API_URL = "https://script.google.com/macros/s/AKfycbyH_a00AH3-DM36sk-lWwFWVzRJkzpT_fx6K9to5CLEmuCRRkPtOIJA_LZVAqs43ux_/exec";

        // 打字机特效函数
        function startTypewriter(textToType) {
            const typeElement = document.getElementById('typewriter-text');
            typeElement.innerHTML = '<span id="text-container"></span><span class="cursor"></span>';
            const textContainer = document.getElementById('text-container');
            
            let i = 0;
            function typeWriter() {
                if (i < textToType.length) {
                    textContainer.innerHTML += textToType.charAt(i);
                    i++;
                    setTimeout(typeWriter, 80); // 打字速度
                }
            }
            setTimeout(typeWriter, 500);
        }

        // 核心数据拉取函数
        async function fetchDatabase() {
            try {
                const response = await fetch(API_URL);
                const data = await response.json();

                // === 1. 渲染 Profile (Hero & About) ===
                if (data.profile) {
                    // 更新名字和简介
                    document.getElementById('profile-name').textContent = data.profile.hero_title || "KK Tan";
                    document.getElementById('profile-desc').textContent = data.profile.hero_desc || "Database connection established. Ready to automate.";
                    
                    // 触发职位打字机特效
                    const roleText = data.profile.hero_subtitle || "Finance Automation Specialist";
                    startTypewriter(roleText);

                    // 更新终端 About 文本
                    if (data.profile.about_text) {
                        const aboutHtml = `<p><span class="prompt">root@kk-tan:~$</span> cat profile.txt</p>
                                           <p>${data.profile.about_text.replace(/\n/g, '<br><br>')}</p>`;
                        document.getElementById('about-container').innerHTML = aboutHtml;
                    }
                }

                // === 2. 渲染 Projects (项目卡片) ===
                const container = document.getElementById('projects-container');
                container.innerHTML = ''; // 清除预加载文字

                if (data.projects && data.projects.length > 0) {
                    data.projects.forEach(project => {
                        // 忽略空行
                        if (!project.title) return;

                        // 处理标签 (按逗号分割)
                        const tagsArray = project.tags ? project.tags.split(',') : [];
                        const tagsHtml = tagsArray.map(tag => 
                            `<span class="project-tag">${tag.trim()}</span>`
                        ).join('');

                        // 生成卡片 HTML
                        const cardHtml = `
                            <div class="project-card glass-panel">
                                <div class="project-visual">${project.icon || '⚡'}</div>
                                <div class="project-body">
                                    <h3 class="project-title">${project.title}</h3>
                                    <p class="project-desc">${project.description || ''}</p>
                                    <div class="project-tags">${tagsHtml}</div>
                                    <div class="project-links">
                                        <a href="${project.link || '#'}" target="_blank" class="btn btn-cyan">View Details ↗</a>
                                    </div>
                                </div>
                            </div>
                        `;
                        container.innerHTML += cardHtml;
                    });
                } else {
                    container.innerHTML = '<p style="color: var(--text-muted); text-align:center; grid-column: 1/-1;">No records found in database.</p>';
                }

            } catch (error) {
                console.error("Database Error:", error);
                document.getElementById('profile-desc').textContent = "System Error: Connection to Google Sheets API failed.";
            }
        }

        // 移动端菜单逻辑
        document.addEventListener('DOMContentLoaded', () => {
            const hamburger = document.getElementById('hamburger');
            const navLinks = document.getElementById('nav-links');
            const links = navLinks.querySelectorAll('a');

            hamburger.addEventListener('click', () => {
                navLinks.classList.toggle('active');
            });
            links.forEach(link => {
                link.addEventListener('click', () => {
                    navLinks.classList.remove('active');
                });
            });

            // 页面加载完成后，立刻发起 API 请求
            fetchDatabase();
        });
    </script>
</body>
</html>
